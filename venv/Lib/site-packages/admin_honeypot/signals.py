from django.dispatch import Signal

honeypot = Signal()
# honeypot_signal = Signal()

# Connect a receiver to the signal
# def my_receiver(sender, **kwargs):
    # Handle the signal
    # pass

# honeypot_signal.connect(my_receiver, dispatch_uid="my_unique_id")
